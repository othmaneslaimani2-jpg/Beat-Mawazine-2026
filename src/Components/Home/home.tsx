import { ConcertCard } from "../Layout/concertCard";

export const Home = () => {
    return (
        <div className="flex items-center justify-center min-h-[calc(100vh-100px)]">
            <ConcertCard />
        </div>
    );
}